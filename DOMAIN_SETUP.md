# ربط النطاق المخصص

تم إعداد روابط الموقع العامة ورموز QR داخل الكود لاستخدام **https://hatfaltmyez.com**. أظهر فحص النطاق أن اسم المضيف لا يحل عبر DNS حاليًا، لذلك لن يفتح الرابط أو QR حتى تكتمل عملية الربط.

| الحالة | الإجراء المطلوب |
|---|---|
| النطاق مملوك مسبقًا | من واجهة إدارة المشروع افتح **Settings → Domains**، ثم اختر ربط نطاق موجود وأدخل `hatfaltmyez.com`، وبعدها طبّق سجلات DNS التي تظهر لك لدى شركة تسجيل النطاق. |
| النطاق غير مملوك | من **Settings → Domains** ابحث عن `hatfaltmyez.com` واشتره إن كان متاحًا، ثم عيّنه نطاقًا للموقع. |
| بعد نجاح الربط | انشر آخر نسخة محفوظة، ثم اختبر الصفحة الرئيسية ورابط واتساب وQR من شبكة جوال خارجية. |

لا يحتاج الكود إلى تعديل إضافي بعد الربط؛ روابط التتبع والفاتورة وQR مجهزة لاستخدام النطاق المخصص تلقائيًا.
