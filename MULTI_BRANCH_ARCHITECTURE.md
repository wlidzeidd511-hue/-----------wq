# بنية منصة هاتف التميز متعددة الفروع

## الهدف

تتحول المنصة من محل واحد وحساب مالك واحد إلى نظام مركزي قابل لإضافة فروع وموظفين وعملاء وإعدادات ورسائل وجوائز دون تعديل الكود. تبقى رحلة الزبون العامة متاحة بلا تسجيل دخول، بينما تُحمى لوحات المالك والموظف والعميل بجلسات مستقلة وصلاحيات واضحة.

## حدود البيانات

| المجال | نطاق العزل |
|---|---|
| الطلبات والفواتير والأجهزة | مرتبطة بفرع واحد وعميل واحد، مع إمكانية عرضها مجمعة للمالك فقط |
| الموظفون | يرتبط الموظف بفرع أساسي ويمكن توسيع الربط لاحقًا لأكثر من فرع |
| العملاء | هوية موحدة حسب رقم الجوال، ويمكن أن يملك العميل طلبات في أكثر من فرع |
| الأرباح والتكاليف | تُحسب حسب الفرع أو مجمعة، ولا تظهر إلا بعد صلاحية المالك والتحقق الإضافي |
| واتساب وخرائط Google | إعدادات مستقلة لكل فرع |
| الرسائل المنبثقة | عامة أو خاصة بفرع، ومقسمة إلى فئات قابلة للإدارة |
| الجوائز والأكواد والتقييمات | مرتبطة بالفرع والعميل والطلب عند الحاجة |
| الحضور المباشر | جلسات قصيرة العمر تحفظ الصفحة الحالية وآخر نشاط دون تخزين بيانات حساسة غير لازمة |

## الأدوار

| الدور | الصلاحيات الأساسية |
|---|---|
| المالك | جميع الفروع، الإعدادات، الموظفون، العملاء، التقارير، الأرباح، الصلاحيات، الرسائل والجوائز |
| الموظف | إنشاء الفاتورة والعميل، إدخال بيانات الجهاز، تصويره ورفع الصور، وتحديث الحقول التي يسمح بها المالك |
| العميل | فواتيره وأجهزته وضمانه وصوره الظاهرة له وحالة الصيانة وجوائزه وأكواده فقط |
| الزائر | التتبع العام الآمن، اتصل بنا، شاشة الانتظار والخدمات دون تسجيل دخول |

## الجداول المستهدفة

| المجموعة | الجداول |
|---|---|
| الفروع والإعدادات | `branches`, `branch_settings` |
| الهوية والصلاحيات | `staff_accounts`, `staff_branch_assignments`, `customers` |
| دورة الخدمة | توسيع `service_orders`, `order_photos`, `order_status_history` وربطها بالفرع والعميل والموظف |
| التدقيق | `audit_logs` |
| الرسائل | `popup_messages`, `direct_messages`, توسيع `notification_messages` |
| الحضور | `presence_sessions` |
| الجوائز | `scratch_campaigns`, `scratch_prizes`, `scratch_codes` |
| التقييم | `service_ratings` |
| النسخ الاحتياطي | `backup_snapshots` لحفظ بيانات النسخة ومكانها وحالتها دون تخزين الملف داخل قاعدة البيانات |

## قرارات الأمان

1. لا تُخزن كلمة المرور الأصلية للمالك أو الموظف أو العميل؛ يُخزن اشتقاق مشفر مع ملح مستقل لكل حساب.
2. جلسات المالك والموظف والعميل منفصلة، وتحتوي على نوع الحساب ورقم الإصدار لإبطال الجلسات عند تغيير كلمة المرور.
3. كل استعلام للموظف يُقيد بالفرع والصلاحية على الخادم، وليس بإخفاء الأزرار في الواجهة فقط.
4. بيانات مثل التكلفة والربح والملاحظات الداخلية ومكان الجهاز لا تدخل مطلقًا في حزمة التتبع أو الفاتورة العامة.
5. جميع العمليات الحساسة تُسجل في سجل التدقيق مع الفاعل والفرع والكيان والوقت.
6. أسرار WhatsApp لا تُخزن كنصوص مكشوفة في قاعدة البيانات؛ تحفظ في إعدادات البيئة، بينما تخزن معرفات أرقام الفروع والقوالب في قاعدة البيانات.

## خطة ترحيل البيانات الحالية

1. إنشاء فرع **البساتين** كفرع افتراضي أولي، وإنشاء فرع **البصيرية** دون بيانات.
2. ربط الطلبات والإعدادات الحالية بفرع البساتين مع الحفاظ على أرقام الطلبات ورموز التتبع والصور والسجل.
3. إنشاء سجلات عملاء من أرقام الجوال الموجودة دون إرسال كلمات مرور تلقائيًا للطلبات التاريخية حتى لا تصل رسائل غير متوقعة.
4. الإبقاء على رابط التتبع الحالي صالحًا، مع إضافة اختيار الفرع وآخر أربعة أرقام في البحث اليدوي.
5. تشغيل اختبارات تحقق بعد كل مرحلة ترحيل قبل الانتقال إلى المرحلة التالية.

## الرسائل المنبثقة

تُحفظ الرسائل في قاعدة البيانات بالفئات: `in_repair`, `ready`, `before_rating`, `after_delivery`, `before_scratch`, `scratch_win`, و`scratch_loss`. يختار الخادم رسالة فعالة عشوائيًا من الفئة والفرع، مع محاولة عدم إعادة آخر رسالة للجلسة نفسها. يمكن للمالك تعديل الرسالة وتعطيلها وتغيير وزن ظهورها أو جعلها عامة لجميع الفروع.

## المهام الدورية

توليد الأكواد الشهري والنسخ الاحتياطي مهام حتمية قصيرة تُنفذ عبر نقطة مجدولة آمنة، مع منطق متكرر آمن يمنع إنشاء حملة الشهر نفسها مرتين. إضافة إلى ذلك، يتأكد إنشاء الفاتورة من وجود حملة الشهر، بحيث يستمر النظام حتى لو تأخر التشغيل المجدول.

## الاستمرارية

مصدر الحقيقة للبيانات هو قاعدة البيانات، وللصور والنسخ هو التخزين السحابي. لا يعتمد النظام على ملفات محلية أو ذاكرة الخادم، لذلك يبقى متوافقًا مع الاستضافة المتدرجة وإعادة تشغيل الخدمة. استمرار الموقع يتطلب بقاء المشروع منشورًا وتجديد النطاق سنويًا.
